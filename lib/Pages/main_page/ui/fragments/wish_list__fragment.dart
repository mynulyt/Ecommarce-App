import 'package:flutter/material.dart';

class WishListFragment extends StatelessWidget {
  const WishListFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Wish List Fragment"),
    );
  }
}
